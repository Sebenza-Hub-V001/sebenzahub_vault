---
title: "Repo Sync — 2026-05-06 (325 commits, migrations 0066–0115)"
type: source
created: 2026-05-06
updated: 2026-05-06
tags: [repo-sync, codebase-audit, trial, pricing, ai-governance, mobile, business-recruiter-alignment, linda, vendor-management, scim, kyc]
sources: []
status: active
confidence: high
---

# Repo Sync — 2026-05-06

**Source:** `C:\Users\User\Desktop\Sebenza_Hub_Claude\Sebenza_Hub_Claude_V2` (local clone), branch `claude/build-sebenza-hub-website-559Nz`
**Window:** 2026-04-20 (last sync) → 2026-05-06 (today)
**Volume:** 325 commits, migrations 0066 → 0115 (+50 SQL migrations).

## Summary

Sixteen days of intense build dominated by eight themes:

1. **30-day free trial system (Phases 7–10)** — schema, signup hook, banner, in-place trial conversion on checkout, admin trial analytics tile, kill switch + per-plan + env duration, security envelope.
2. **Pricing tier build (Phases 6–7)** — overage billing, invoice line items, admin-managed credit packs, 14-day default trial overrides.
3. **AI governance & cost** — daily AI spend circuit breaker, runtime budget overrides + kill switch, Spend Explorer (per-feature × per-model breakdown), ZAR-alongside-USD across all cost tabs, default model bumped to **gpt-4o**, `isAgentic` flag for multi-turn features, platform-wide AI default chain, AI-driven SQL migration generation from natural language.
4. **Mobile app rollout** — full iOS + Android scaffolds with biometric auth, push notifications (APNs + FCM), Universal Links / App Links, splash screens, project version 2, Camera/Photo permissions, Individual portal screens wired to live auth and live endpoints.
5. **Linda agent-runtime as the only path** — `FEATURE_LINDA_AGENT_MODE` flag removed entirely. The agent runtime now powers all WhatsApp logic; legacy code path retired. Plus "Chat to Linda" free-form AI chat, auto-matching, invite-to-register linking, block/unblock numbers, coaching/screening scripts panel, dynamic-button template handling, daily Meta template sync, ai_usage_logs tool_calls audit trail.
6. **Business–Recruiter Alignment Phase 1** — migration `0110_business_recruiter_alignment_phase1.sql` plus a new wave of Business surfaces (BusinessBrandProfile page, BusinessIntegrationsApi page with API-key + webhook management, Cost Center management, SCIM 2.0 provisioning, Succession Planning workflows, Vendor Management with performance tracking).
7. **Recruiter public profile** — slug uniqueness enforced (`0108`/`0109`), expanded agency block (description, contact, website, social), badges/certs/awards/team rendered as objects (with crash fix), recruiter's active jobs surfaced on the public profile.
8. **CV Reviews, Interview Studio, Salary Calculator, KYC, Domain Verification, Calendar two-way sync, Background Check provider** — large feature drops across Individual and Business surfaces.

## Schema Changes

**Migrations added in this window:** 0066 → 0115 (50 migrations). Highlights below; full list at end of section.

| # | Migration | Area |
|---|---|---|
| 0066 | `add_scrape_details_to_job_aggregator_configs` | Jobs aggregator |
| 0067 | `add_missing_source_column_to_job_aggregator_and_align_whatsapp_template_quality` | Jobs / WhatsApp |
| 0068 | `fix_annual_plan_prices_to_full_year_amount` | Billing |
| 0069 | `add_industry_subindustry_columns_to_jobs` | Jobs (AI classification) |
| 0070 | `add_source_to_job_applications` | Application source attribution |
| 0071 | `add_error_capture_to_whatsapp_messages` | Linda observability |
| 0072–0073 | `add_cv_reviews_table_for_saved_ai_cv_review_snapshots` | Individual CV Reviews |
| 0074 | `add_unique_constraint_on_ai_feature_configs_feature_key` | AI governance |
| 0075 | `make_candidate_credentials_template_id_nullable` | Recruiter assessments |
| 0076 | `add_candidate_id_and_phone_to_screening_candidates` | Screening |
| 0077 | `add_block_columns_to_whatsapp_conversations` | Linda block/unblock |
| 0078 | `widen_whatsapp_media_type_to_127` | WhatsApp media |
| 0079–0080 | `update_ai_feature_config_defaults_to_gpt_4o` / `class_based_defaults` | AI governance |
| 0081 | `kill_deprecated_ai_features` | AI governance cleanup |
| 0082 | `add_credit_type_column_candidates_uploaded_feature_for_top_up_pilot` | Credits |
| 0083 | `org_keyed_credit_wallets_candidates_uploaded_feature` | Credits |
| 0084 | `force_source_on_job_applications_re_apply_0070` | Source attribution |
| 0085–0086 | `add_credit_packs_table` | Admin credit packs |
| 0087 | `update_default_trial_to_14_days` | Trial |
| 0097 | `re_apply_job_applications_source_column` | Source attribution |
| 0098–0099 | `add_token_hash_and_expires_at_to_team_members` | Team invites |
| 0100 | `add_candidate_import_sessions_table` | Recruiter bulk import |
| 0101 | `add_audio_url_audio_duration_seconds_attachments_to_call_notes` | CRM call notes |
| 0102–0103 | `alter_table_organization_integrations` / `add_video_provider_hris_provider` | Integrations |
| 0104 | `add_full_name_generated_column_to_users` | User schema |
| 0105 | `add_orchestration_run_id_and_evaluation_version_to_screening_evaluations` | Screening |
| 0106 | `add_missing_target_columns_to_bbbee_scores` | Compliance |
| 0107 | `add_ai_budget_overrides_table_for_runtime_daily_budget_kill_switch` | AI governance |
| 0108–0109 | `add_unique_index_on_recruiter_profiles_public_profile_slug` | Recruiter profile |
| 0110 | `business_recruiter_alignment_phase1` | Business–Recruiter alignment |
| 0111 | `phase3_business_features` | Business |
| 0112 | `phase4_production_hardening` | Platform |
| 0113 | `phase5_integration_links` | Integrations |
| 0114 | `add_renewal_columns_to_subscriptions_and_re_apply_ai_recurring_monitors` | Billing renewals |
| 0115 | `widen_user_education_text_columns_to_255` | User schema |

Several earlier migrations (`0054_add_my_field_to_users`, `0064_add_audit_logs_table_for_general_purpose_audit_trail`, `0065_add_invite_phone_to_magic_link_tokens`, mobile_sessions, whatsapp_conversations.active_window_id, organizations.domain_verification, mentorship/wiki/billing tables, ai_feature_catalog updates) also landed in this window without sequential numbering; treat the migration count as **+50 minimum** rather than the strict 0066–0115 range.

## Key Changes (Commit-Level Highlights)

### Free Trial System (Phases 7–10) — `ceea3ec9`, `6091f089`, `58d83b70`, `facdf53c`, `85d0c37a`, `5696255e`, `0c098a98`

- **Phase 7 (UI):** `TrialBanner` + global `UpgradeDialog` surfaced across the app.
- **Phase 8 (conversion):** Trial subscription converts in-place at checkout — no second subscription record, no plan-jump glitches.
- **Phase 9 (analytics):** Admin trial analytics tile + `/api/admin/trials/analytics` endpoint.
- **Phase 10 (admin controls + security):** Kill switch, env-controlled trial duration, per-plan trial flags, full UI controls; E2E spec for trial envelope, admin analytics, security.
- **Default trial duration:** 14 days (migration `0087`). Was 30 in earlier phases — now configurable per-plan with 14-day default.
- **Email previews** for usage alerts and pricing cards (`0c098a98`).

### Pricing Tier Build (Phases 6–7) — `f75fd2d5`, `fcb2b0d2`, `40749e4e`, `a9fe414f`, `ea40e303`

- **Phase 6:** Overage billing with invoice line items.
- **Phase 7:** Quota gating manual overrides + smoke tests for agentic AI features.
- **Admin credit packs (`40749e4e`, `0085`/`0086`):** Admin can now author and price credit packs from the UI.
- **Plans + features export** to `.xlsx` workbook (`a9fe414f`).
- **Magic-link domain verification + recruiter profile defaults** updated (`84179b8b`).

### AI Governance & Cost (major)

- **Default model → gpt-4o** (`d07b994b`, migrations `0079`/`0080`). All `gpt-5.4` references swapped to `gpt-4o` across the application; `ai_feature_configs` defaults updated to class-based (rather than per-feature hardcoded).
- **Daily AI spend circuit breaker** (`df49ea48`) — automatic throttle when daily spend crosses threshold.
- **Runtime daily budget kill switch** (`756aaa7e`, migration `0107`) — admin can override the budget at runtime; `ai_budget_overrides` table backs the override.
- **Spend Explorer admin tab** (`81afeaa9`) — per-feature × per-model cost breakdown for admin observability.
- **AI Off toggle now actually disables** the feature (`92991158`) — previously cosmetic.
- **ZAR alongside USD** (`6b4bf562`, `9cf b7d53`, `7c78c3d8`) — Predictive, A/B Testing, Usage Analytics, Cost Intelligence, Cost Optimizer all show ZAR equivalent.
- **Cost-per-request weighted by call count** (`cb289274`) — was arithmetic mean; now volume-weighted.
- **Blended `token_cost_per_1k`** exposed for Model Cost Comparison (`cc3e13dc`).
- **`isAgentic` flag** added to AI features (`62ae13b8`) — distinguishes multi-turn / adaptive features from one-shot inference.
- **Platform-wide AI default chain** (`eafcde2f`) — admin sets a global default chain; per-feature configs override only when set.
- **Global config priority** in model resolution (`f5c5cd25`) — overrides hardcoded defaults.
- **AI metrics enhancement** (`5273905f`) — fallback attempt tracking, weighted averages by request volume.
- **AI migration generation** (`f59b82ad`) — admin authors SQL migrations from natural language; `471b8130` adds the admin migrations UI; `12d5d1a0` adds password unlock for the UI.
- **Database migration guard** (`a2c65869`) — destructive `drizzle-kit push` blocked in production.
- **AI feature catalog** entries updated (`22adac3b`, `10771df1`).
- **Limit candidate scoring to 50** for performance (`07721448`).
- **CV review sub-tools** added; feature catalog updated (`b45bdc8d`).
- **`unique_constraint_on_ai_feature_configs.feature_key`** enforced (`0074`) — prevents duplicate configs.

### Linda / WhatsApp (significant architectural shift)

- **`FEATURE_LINDA_AGENT_MODE` flag removed entirely** (`12f25545`, PR #606 merge `0fe168c3`) — agent runtime is the only WhatsApp path. Legacy state-machine code retired.
- **Agent runtime tests** (`ae458ebd`) — proper test coverage for Linda agent run options.
- **Fire-and-forget audit logging for agent runs** (`32a2dcc0`) — runs are written to `ai_usage_logs`.
- **`tool_calls` audit trail in `ai_usage_logs`** (`9be43966`) — every tool invocation by Linda is logged. Linda agent tools for WhatsApp also implemented in this commit.
- **"Chat to Linda" free-form AI chat option** in main menu (`238548ce`) — open-ended conversation alongside structured flows.
- **Linda Chat documented + Main Menu refresh** in flow visualizer (`707fdcef`).
- **Auto-matching for WhatsApp conversations** (`d4bd5db3`) — incoming numbers auto-match against existing users; invite registration available when no match.
- **Block/unblock numbers** (`e8e53c72`, migration `0077`) — admin controls in command center.
- **Coaching and screening scripts panel** (`8ec06ade`) — recruiter-facing helper for live conversations.
- **Dynamic template buttons** (`7e304c24`) — Meta template message handling supports CTA buttons.
- **Daily Meta template sync at 06:00 SAST** (`11e64998`) — keeps WhatsApp template approval state fresh.
- **Audience counts API** (`98081a11`) — broadcast targeting now has live counts.
- **Invite-to-register linking via WhatsApp** (`fd2955ea`, migration `0065`) — magic-link tokens carry `invite_phone`.
- **Detailed failure reasons + codes** (`f706c35c`, migration `0071`).
- **Slack notifications for new + returning conversations** (`d97ad107`) — formatted with conversation context.
- **WhatsApp CV staging for unregistered uploads** (`e146a6ac`) — uploads persist instead of being lost to anonymous senders.
- **Mind Map and prior 20 admin tabs** unchanged — see [[01-entities/whatsapp-bot]].

### Business — Recruiter Alignment Phase 1

Migration `0110_business_recruiter_alignment_phase1.sql` plus migrations `0111` (phase 3 business features), `0112` (phase 4 production hardening), `0113` (phase 5 integration links).

- **`BusinessBrandProfile` page** (`434c6332`) — public-facing employer brand profile, mirrors Recruiter's agency profile pattern.
- **`BusinessIntegrationsApi` page** (`434c6332`) — API key management + webhook management.
- **Cost Center management** (`e1d0f8da`) — full CRUD for cost centers, hooked into approvals + reporting.
- **SCIM 2.0 provisioning routes** (`c97c18fb`) — enterprise IdP integration for user provisioning.
- **Succession Planning workflows** (`c97c18fb`) — formal workflow for identifying and grooming successor candidates per role.
- **Vendor Management with performance tracking** (`c97c18fb`) — Business-side view of recruiter vendor performance (fill rates, time-to-fill, quality scores).
- **Salary Calculator route** added to BusinessDashboard (`de61788e`); endpoints + core logic at `802786e3`; tax year updated to 2026 (`81f374e0`).
- **Calendar integration** — two-way sync with Google Calendar + Microsoft 365 (`f40939b4`, `4b54b084`).
- **Background check provider integration** (`1dc6af1c`) — first external BG check provider wired in.
- **Job moderation features** (`45c0d1e8`) — Business can flag/moderate jobs visible to their org.
- **Business pages** added to App routing (`6fbcc6a4`).
- **Business Dashboard navigation** simplified for clarity (`38475907`, `ef286295`).

### Recruiter

- **Public profile expansion (`892fa559`, `92c2d6b6`):**
  - Agency block adds description, contact, website, social.
  - Badges/certs/awards/team now rendered as object types (with `8308245d` crash fix for malformed objects).
  - Recruiter's active jobs surfaced on the public profile.
- **Public-profile slug uniqueness check** (`bd959266`, migrations `0108`/`0109`).
- **Job-detail link path corrected** to `/job/:id` singular (`5f2ffa1c`).
- **Search & Talent Pool Intelligence** (`21d724bb`) — AI-driven search and talent-pool features.
- **Bulk candidate import with column mapping** (`59411532`); candidate import sessions table at migration `0100`.
- **Email templates `appliesTo` field** (`150ba2b5`) — controls which dashboards a template appears in.
- **WhatsApp verification flow for recruiters** (`88e33d11`) — normalises parsed resume data and adds verification.
- **Industry / sub-industry handling** in recruiter job creation (`daf2eae9`).
- **Candidate dossier page + assessment routing** (`d85cc0bf`) — full dossier view with credentials backfill for completed assessments.
- **Recruiter team page enhancements** — invite mutation payloads, dialog descriptions (`034c3144`).
- **Trial banner + upgrade flow** in recruiter UI (part of trial Phase 7).
- **Bulk actions** (`f15fb66c`) — admin bulk plan assignment + verification across businesses + recruiters.
- **RecruiterDashboard nav alignment** with manual ordering (`28359576`) — already documented in 2026-04-28 wiki entry.

### Individual

- **CV Reviews — saved snapshots** (`07c0b51f`, `04ff3ae4`, migrations `0072`/`0073`):
  - Multi-tab dashboard with 14 distinct analyses.
  - Save / load / rename / delete CV review reports.
  - Auto-save reports + prominent status banner (`4a110a43`).
  - Before/after transformation handling with feedback + retry option (`b9f1d4c0`).
  - Sub-tools added (`b45bdc8d`).
- **Interview Studio** (`18f37ab3`) — Mock Interview, STAR Builder, Rehearsal Review tabs.
- **Salary Calculator** (`81f374e0`, `802786e3`) — tax year 2026, historical calculation support; routed into Individual + Business dashboards.
- **AI match score teaser + unlock** for job listings (`821c7c69`) — paywall pattern for non-premium users.
- **`ApplyStepperMode` component** (`ded4f1f0`) — guided multi-step apply flow.
- **External job application** redirection + retrieval (`b0da16f3`); native vs external handling clarified (`acb422c5`).
- **Modern CV template** added to CV Builder (`6d03516f`).
- **PDF text extraction + re-parsing for CVs** (`76a06e72`).
- **CV deletion audit log** + bulk delete confirmation (`66324c5b`).
- **Mobile Individual portal** wired to live auth, live endpoints, real data:
  - `4bcc43df` — initial portal screens (mock data).
  - `25a56ba6` — wired to real auth + Camera/Push SPM deps.
  - `58d47d22` — Home/Jobs/Features mapped to real data.
  - `aa36ccf2` — cold-launch splash + Universal Links + Profile API mapping.
  - `171cb11d` — missing mobile portal menu items wired to live endpoints in `Profile.tsx`.
- **Hide UsageMeter for admin users** (`e67cbce3`).
- **Saved-jobs query** uses `saved_at` for recent activity tracking (`67bb73b2`).

### Mobile Apps (iOS + Android)

- **iOS scaffold:** initial project structure with LaunchScreen + storyboards (`55d959dc`, `8c1c022a`); development team + display name (`902697b2`); Xcode workspace + Swift package management (`8ad72262`); build settings (`8279fb75`); project version 2 + camera/photo permissions in Info.plist (`e67900b3`).
- **Android scaffold:** Gradle wrapper + config (`193fcd04`); App Links + deep link handling (`4598e297`).
- **Biometric authentication** (`61da36d6`); `@aparajita/capacitor-biometric-auth` upgraded to 10.0.0 (`bd061ae7`).
- **Push notifications** — APNs (iOS) + FCM (Android) integration with admin endpoints for testing (`cf3e7143`).
- **Splash screens** — light + dark mode assets (`15f139c1`, `6ebc975d`).
- **Universal Links + Profile API mapping** for cold-launch (`aa36ccf2`).
- **Capacitor compatibility** — Camera/Push SPM deps (`25a56ba6`); `@capacitor/assets` dropped to clear 8 high-severity alerts (`f5009feb`); allowed origins updated for localhost dev (`0b9c425a`); Capacitor imports for native build compat (`4f228f88`).
- **Production-native build scripts** + deep-link hosts (`2c7a303a`).
- **Mobile employer pages** added (`e1e89cc7`).
- **Mobile sessions migration** (`e7de1a64`).
- **Deep link logging + error handling** (`47b10316`, `57104eb3`, `d712ea25`).
- **Role switch buttons** for mobile with shorter labels + service worker handling for native builds (`4a50ba5c`).
- **Viewport meta tag** updated for responsive design (`e056db03`).

### Knowledge / How-To Guide

- **How-To Guide dropdown in Header** with audience-specific links (`d68b03eb`, latest commit before sync).
- **How-To Guide link** added to navigation (`e7590c9d`); position updated (`74c1559d`).
- **Analytics tracking** for How-To Guide link clicks (Header + DashboardLayout) (`0273c24c`).
- **Solutions dropdown menu** added to Header (`e83d2685`); ChevronDown icon support (`260e57cb`); reorder for navigation consistency (`4b3cfaa5`).

### Authentication & Email

- **Magic-link domain verification** for email (`84179b8b`) — verifies sender domain before relying on magic-link redirects.
- **Interstitial sign-in page** for magic-link verification (`6bb4900c`) — prevents stale-token confusion.
- **`/auth/verify` GET method restored** (`a1d094a5`) — magic-link re-enabled.
- **Token verification endpoint** changed GET → POST (`6bb4900c`) — security improvement.
- **Deep-link target preservation** across login round-trip (`d97ed76e`).
- **`crossOriginOpenerPolicy`** added to prevent `ERR_BLOCKED_BY_RESPONSE` on email redirectors (`cd2437f8`).
- **Email templates redesigned** with stacked CTA buttons + dark mode + light mode adjustments (`baa34ca3`, `4373cbe8`).
- **Email change propagation** across related tables, with admin notifications (`f3c62f7c`).
- **Email Health diagnostics page** in Admin Dashboard (`3f98e49a`); admin route for magic-link delivery pipeline health (`d0b11efd`).
- **Daily Health page** + collectors + renderers + bearer-token internal endpoint (`371126b0`, `aaf85e6c`, `009b8755`).

### KYC, Domain Verification, Audit

- **KYC verification flow with Smile ID integration** (`4743d931`).
- **Domain verification for organizations** (`ae87006b`, `45c0d1e8`) — verifies an org actually owns a claimed email domain.
- **General-purpose audit logs table** (`272999a7`, migration `0064`) — formal audit trail beyond the existing `audit_trail` table.
- **System-wide orphan audit endpoint** (admin) (`7266642b`).
- **One-off CV-recovery notification endpoint** (admin) (`4967375e`).
- **Soak-report endpoint for agentic AI** (bearer-token) (`d9033dcf`).
- **Slack error notifier** (`18c9605c`) + AI migration prompt examples.
- **Slack alerts tagged by env** (`fd626cc2`); WhatsApp Slack notifications enhanced with detailed message formatting (`d97ad107`).
- **POPIA actions consolidated** in the Privacy tab (`d85cc0bf`) — for discoverability.

### Jobs & Aggregator

- **AI auto-classification of industry / sub-industry** on imported jobs (`b1a09d09`, migration `0069`); job allocation queries enhanced with industry resolution (`05119f5f`).
- **AdminJobs:** unclassified jobs handling + AI classification trigger (`217bffca`); job allocation stats + industry breakdown (`204ccfa1`); server-side filtering with facets endpoint (`03fb0adc`); enrich with org + creator details, delete functionality (`e1768118`).
- **Job aggregator duplicate detection + cleanup** (`999620e9`, `c9dc2188`).
- **Exponential backoff** for transient errors in Adzuna/CareerJet calls (`aa408e94`).
- **`scrapeDetails` option** in aggregator config (`5d9966a0`, migration `0066`).
- **Annual plan prices** fixed to full-year amount (migration `0068`).
- **Salary banding** + improved metrics display (`eb6c46b4`); `JobDetailDrawerV2` redesign (`df867eaa`).
- **AI batch-match scores + user preferences** integrated (`51a6d715`).
- **Job status handling** + content lifecycle status options (`20296427`).
- **Industry / sub-industry selection** in job forms + title components (`e8fddce4`).

### Admin

- **Spend Explorer tab** — see AI Governance.
- **AdminUserSubscriptionPanel** for managing user subs (`0580be78`).
- **System-wide orphan audit** + CV recovery notification + soak-report — see Audit.
- **Bulk actions for businesses + recruiters** — plan assignment + verification (`f15fb66c`).
- **Admin migrations UI** with password unlock + AI generation (`471b8130`, `12d5d1a0`, `f59b82ad`).
- **Email Health page** + magic-link diagnostics — see Email.
- **Daily Health page** — see Email.
- **Trial analytics tile** — see Trial.
- **Admin diagnostics + AI usage reporting** enhanced (`74786cd4`).
- **Header SPA support** with internal routing (`ea40e303`).
- **Daily Health webhook** — Railway → Slack deploy notification relay (`a665f25a`).
- **Domain verification + job moderation** (`45c0d1e8`).
- **Architecture gotchas + conventions** added to CLAUDE.md (codebase-internal — `ce7a74a0`).

### Compliance

- **B-BBEE missing target columns** added (migration `0106`); B-BBEE score mutation now calculates total score correctly (was incomplete in 2026-04-20 sync).
- **POPIA actions consolidated** in Privacy tab (see Audit).
- **Audit logs general-purpose table** (see Audit).
- **CV deletion audit logging** + bulk delete confirmation (see Individual).

### Infrastructure & CI

- **Phase 4 production hardening** (migration `0112`); Phase 4 service files committed (`0bcab2c4`).
- **Phase 5 integration links** (migration `0113`); Phase 5 features for vendor management + compliance (`c36d19c5`).
- **Phase 6 admin observability + notification integration + E2E test coverage** (`c02dcf0c`).
- **CI E2E tests** added across the recruiter sidebar — Place & Retain (`5ad34e4b`, `f20b00ef`); Communication & Automation, Compliance & Governance (`3cf20f6f`).
- **CV Builder smoke spec** + port notes (`2d299102`).
- **Postgres service for E2E tests** in CI (`5b5e79f1`); resolves Postgres errors in daily-health collectors + job aggregator sync (`644c47e5`).
- **Static analysis error handling improved**; legacy URL redirects (`5b5e79f1`).
- **Resume parse worker** — claim failure backoff and logging (`d8f2d6e8`); async parse pipeline + cross-provider AI fallback (`adce4fbd`).
- **Internal Postgres alias** preferred for session pool (`88aa74e7`); silence pool-after-end spam in workers (`b042171f`).
- **Allow custom origins** for Capacitor compatibility (see Mobile).
- **Routes wiring audit + site health + migration + capacitor-debug skills** (`a874bc31`).
- **Pre-merge CI** continues from prior sync; this window adds Postgres-backed E2E.
- **xlsx pin** to 0.20.3 + CI Node 24 (`7caa6a42`).

### Internationalisation / Branding

- **Satoshi font** replaces Montserrat across the application (`267adcb4`).
- **AI progress messaging system** with multi-phase feedback + integration across components (`6d797661`, `eaa9f3fd`).

### Models / Versions

- **Default chat model:** `gpt-5.4` → **`gpt-4o`** (`d07b994b`).
- **Project version → 2** (mobile, `e67900b3`).

## Pages to Update

- [[01-entities/individual-user]] — CV Reviews saved snapshots, Interview Studio (Mock/STAR/Rehearsal), Salary Calculator (tax year 2026), AI match teaser/unlock, ApplyStepperMode, Modern CV template, mobile portal live data
- [[01-entities/recruiter-user]] — public profile expansion (slug, agency block, badges/certs/awards as objects, active jobs), Search & Talent Pool Intelligence, bulk candidate import, candidate dossier, email-template `appliesTo`, trial banner
- [[01-entities/business-user]] — Brand Profile, Integrations API, Cost Centers, SCIM 2.0, Succession Planning workflows, Vendor Management performance tracking, Calendar two-way sync, BG check provider, Salary Calculator, BusinessRecruiter Alignment Phase 1
- [[01-entities/whatsapp-bot]] — Linda agent runtime as the only path (flag removed), Chat to Linda, auto-matching, block/unblock, dynamic template buttons, daily Meta sync, coaching/screening scripts panel, tool_calls audit
- [[02-concepts/ai-features]] — gpt-4o default, isAgentic flag, daily spend circuit breaker, runtime budget kill switch + override, Spend Explorer, ZAR display everywhere, AI migration generation, AI Off toggle fix, platform-wide default chain, fallback attempt tracking
- [[02-concepts/billing-system]] — 30-day → 14-day default trial, trial Phases 7–10, overage billing, admin credit packs, plans/features xlsx export, BusinessIntegrationsApi paywall touchpoints
- [[02-concepts/compliance]] — KYC (Smile ID), domain verification for orgs, general-purpose audit_logs table, POPIA actions consolidated in Privacy tab, B-BBEE total score fix
- [[15-dashboards/business-dashboard]] — new pages: BusinessBrandProfile, BusinessIntegrationsApi, Cost Centers, Salary Calculator (paths to confirm against routing diff)
- [[01-entities/sebenza-hub]] — table count delta (820 → ~870 estimated, needs `grep -c pgTable`), migration count 37 → 87, default model gpt-4o, mobile apps (iOS+Android v2)
- [[02-concepts/tech-stack]] — Capacitor for native builds, Satoshi font, Postgres-backed CI E2E

## Contradictions Resolved

- **Linda Agent Mode flag** — the [[09-sources/repo-sync-2026-04-20]] noted Linda's AI Phase 0–6 expansion behind shared helpers + intent fallback. This sync removes `FEATURE_LINDA_AGENT_MODE` entirely — agent runtime is the only path. The 2026-04-20 record described a dual-mode system; that's no longer accurate.
- **Trial duration** — earlier records implied a 30-day trial; the actual default is **14 days** (migration `0087`), with Phase 10 making it env-controllable + per-plan + kill-switchable.
- **Default AI model** — Claude Opus 4.7 stayed put for Anthropic features, but the OpenAI default moved from `gpt-5.4` to `gpt-4o` across the application.
- **B-BBEE total score** — the 2026-04-20 sync noted score-mutation improvements; this window adds the missing target columns (migration `0106`), closing the gap.

## Open Questions Raised

- Is the **agent-mode-only Linda** running in production for all tenants, or behind a percentage rollout? The flag is gone from code but operational state isn't visible in the diff.
- **Mobile apps** — are iOS and Android apps published to the App Store / Play Store, or build-only with internal TestFlight / internal Play tracks?
- **SCIM 2.0** — which IdPs is Sebenza Hub testing against (Okta, Azure AD, JumpCloud)? Is provisioning live for any pilot tenant?
- **Cost centers** — are these wired into the offer-approval flow (so spend per cost center is tracked) or report-only?
- **AI budget kill switch** — what's the operational runbook for triggering it? Who has admin permissions?
- **Background check provider** — which provider was integrated, and is it South Africa-resident or US/EU-based (POPIA implications for cross-border data flow)?
- **Default trial 14 days** — was the 30-day trial referenced in marketing materials before this change? Any user-facing reconciliation needed?
- **Domain verification** — what's the verification mechanism (DNS TXT, file upload, email)?
- **Vendor performance tracking** — does it surface to Recruiter (so they can see how Business clients rate them) or is it Business-only?

## References

- [[09-sources/repo-sync-2026-04-20]] — Prior sync (151 commits)
- [[09-sources/repo-audit-2026-04-07]] — Baseline full audit
- [[09-sources/sa-ai-policy-compliance-review-2026-04-11]] — POPIA / SA AI Policy compliance review
- [[09-sources/whatsapp-bot-training-manual-v2-2026-04-11]] — Linda v2 manual (now partially superseded by agent-mode-only architecture)
- [[log.md]] — Activity log

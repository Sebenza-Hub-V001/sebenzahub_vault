---
title: "Repo Sync — 2026-04-20 (151 commits, PRs #295–#311+)"
type: source
created: 2026-04-20
updated: 2026-04-20
tags: [repo-sync, codebase-audit, whatsapp, popia, ai, flow-builder, pay-equity, individual-dashboard]
sources: []
status: active
confidence: high
---

# Repo Sync — 2026-04-20

**Source:** `C:\Users\User\Desktop\Sebenza_Hub_Claude\Sebenza_Hub_Claude_V2` (local clone), branch `claude/build-sebenza-hub-website-559Nz`
**Window:** 2026-04-11 (last sync) → 2026-04-20 (today)
**Volume:** 151 commits, PRs #295 through #311+ and subsequent direct commits.

## Summary

Nine days of dense work dominated by five themes:

1. **WhatsApp AI expansion (Phase 0–6)** — Linda gains real AI intelligence: default intent fallback, RAG-powered FAQs, escalation summarisation, Whisper voice transcription, AI reply drafts, bidirectional auto-translation.
2. **Flow Builder maturation** — from drag-and-drop prototype to a production runtime with versioning, simulator, analytics, cron triggers, event wiring, localization, 10 new action types, and a two-tab (My Flows / Permanent) editor.
3. **POPIA hardening** — AI-specific consent rolled out end-to-end: signup → application → privacy policy page → AI screening disclosure → opt-in demographic capture → signup-consent persistence.
4. **Individual dashboard de-hardcode** — mentorship, micro-learning, peer-teaching, job-linked learning, study planner, skill tree, portfolio, learning analytics, interview practice — all nine areas switched from demo data to real server-driven endpoints.
5. **Security & infra** — 3 Dependabot patches, `crypto.randomBytes` for share/access tokens, role-gating of 5 AI-intelligence endpoints, pre-merge CI (tsc + static-analysis + smoke e2e), proper SQL migration runner that auto-applies on boot, readiness endpoint.

## Schema Changes

**Tables:** 794 → 820 (+26, per `grep -c pgTable shared/schema.ts`).

**New migrations (0028 → 0036):**

| # | Migration | Area |
|---|-----------|------|
| 0028 | `add_intent_detection_config` | WhatsApp bot |
| 0029 | `agent_teams_quality_gates` | Admin agent teams + AI quality gating |
| 0030 | `whatsapp_command_center_tables` | Admin WhatsApp command centre |
| 0031 | `backfill_menu_prompt_faq` | WhatsApp FAQ/menu seed |
| 0031 | `widen_corporate_client_contacts_full_name` | CRM schema fix |
| 0032 | `whatsapp_flow_templates` | Flow Builder templates |
| 0033 | `users_phone_index_and_wa_magic_link` | WhatsApp magic link auth |
| 0034 | `whatsapp_ai_expansion` | AI helpers + FAQ/RAG tables |
| 0035 | `enable_whatsapp_intent_ai` | Default-on intent fallback |
| 0036 | `recruiter_whatsapp_fields` | Recruiter WA profile fields |

## Key Changes (Commit-Level Highlights)

### WhatsApp Bot — Linda

**AI Expansion (Phase 0–6, all 2026-04-19):**
- **Phase 0** (`4a10b2d`) — Shared helpers + schema foundation (`0034`).
- **Phase 1** (`db1262c`, `0035`) — Intent AI fallback **enabled by default**: Linda now routes unmatched messages through LLM intent classification instead of dumping to menu.
- **Phase 2** (`49e40e9`) — RAG-powered FAQ answers sourced from `wiki_pages` table. Linda can answer open-ended questions grounded in wiki content.
- **Phase 3** (`61d0e0c`) — Auto-summarises the full conversation when escalating to a human agent. Reduces context-switch burden in Team Inbox.
- **Phase 4** (`9d38c8d`) — Voice notes transcribed via **Whisper** before NLU processing.
- **Phase 5** (`c689d31`) — AI-suggested reply drafts in the inbox composer (agent-side copilot).
- **Phase 6** (`f6b8c51`) — Bidirectional auto-translation between user and agent — user writes isiZulu, agent reads English, agent replies English, user receives isiZulu.

**WhatsApp UX:**
- **Magic link auth** (`7b74360`, `0033`) — Users authenticate WhatsApp by phone lookup; if missed, capture email (`fa5508c`) and link the number.
- **Account-type picker** (`1e2f941`) — Greeting flow asks Individual vs Recruiter vs Business before routing.
- **Job filters** (`d3ea390`, `9b3e6ae`) — Full filter-menu parity with web: Province, Industry, Type, Arrangement.
- **Localization** (`edaa142`) — Bot messages and interactive responses support multi-language at the response layer (not just greetings).
- **Mind Map tab** (`ffa50dc`) — Interactive conversation mind-map with add/move/delete nodes.
- **Bug fix** (`10ce572`) — Stopped fabricating `intent_confidence` in analytics; now reports real values only.

### Flow Builder (major Phase 2 + Phase 3)

- **Runtime override** (`aaedf09`) — Flows take precedence over the hardcoded state machine; content-managed conversation logic is now authoritative.
- **Phase 2** (`59a4359`) — 10 new action node types + delay handling + flow instance tracking.
- **Phase 3 backend** (`4722fa6`) — Versioning, event subscription, simulator, analytics tables/endpoints.
- **Phase 3 frontend** (`d764e26`) — Simulator + analytics dialogs + live-instance badges.
- **Cron triggers** (`6ee12c2`) — 7 cron-based flow triggers with dedupe ledger.
- **Backend event wiring** (`ae333a2`) — Platform events now fire into flow runtime.
- **Editor UX** — Drag-reorder + connection editing (`06d043d`), editable positions + per-connection conditions (`120c7dc`), template preview/edit before use (`b786477`), Permanent tab (read-only hardcoded flows, `6d79309`), Duplicate-to-My-Flows (`3e4d2fe`), full editor via Edit button on My Flows (`b4540ef`).
- **Templates** — 5 new seed templates (`0560d54`), flow templates migration + seeding (`4acb5c0`).
- **Starter seeds** (`ebf57a9`) — Starter auto-responders, intents, and FAQs ship with the app.

### POPIA / Compliance

- **AI screening consent enforcement** (`34ef400`) — `POST /api/applications` rejects without `ai_processing` consent flag.
- **Consent checkbox in QuickApplyDialog** (`ca7442e`) — Candidate-facing AI disclosure before apply.
- **AI-assisted review badge** (`2b4f7bb`) — Public job detail page discloses that AI is used in reviewing applications.
- **Privacy Policy page** (`02fc293`) — POPIA-aligned scaffold at `/privacy`.
- **Signup consent flow** (`bb48a71`, `3c6073e`) — Signup consent persists to `popia_consents` on magic-link verify, with clarified AI-in-copy.
- **Demographic opt-in** (`28ffc60`) — Gender/race demographics now opt-in at capture; gender/race pay-gap cards re-enabled on the back of consented data.

### Platform Pay-Equity Dashboard

- **Admin rollout** (`b104376`, 2026-04-19) — Platform-wide pay equity dashboard for the Admin layer.
- **Recruiter** (`4b6a5b1`) — Hardcoded data replaced with real benchmark + pay-equity endpoints.
- **B-BBEE** (`b9885d0`) — Score mutation now calculates total score and schema updated for targets.

### Individual Dashboard (de-hardcode pass)

All 2026-04-18, greenfield real-data implementations replacing demo shells:
- `e3113f0` — **Mentorship** engagements (real, with direct-apply migration script `6bd6e05`)
- `4589480` — **Interview** self-practice sessions backed by DB
- `09952db` — **Learning analytics** — real activity + completion tracking
- `deec1f8` — **Portfolio** — real project CRUD + derived impact heatmaps
- `901096c` — **Skill tree** — server-driven node progress, quests, XP
- `2b3705b` — **Study planner** — real blocks, goals, logs, resources
- `769f7ff` — **Job-linked learning** — real saved-jobs + learning junction
- `7d53a6f` — **Peer teaching** — real offerings, lessons, study groups
- `8821d47` — **Micro-learning** — greenfield lessons, SRS flashcards, SM-2 algorithm server-side
- **5 re-added sections** (`11e6c39`) across 3 pages (dropped during refactor, re-added after audit).
- **Verify script** (`f40c4db`) — Migrations for all 21 new Individual dashboard tables verified.

### Recruiter Dashboard

- **Team management** (`02ad802`, `28771d5`, `0eec7f8`) — Team acceptance flow, owner designation, invitation enhancements, recruiter team page.
- **Smart match** (`358bda8`) — Match candidates to specific job postings.
- **AI candidate search** (`4330f08`) — Keyword filtering + enriched result fields.
- **AI-intelligence endpoints role-gated** (`f31a58a`) — 5 endpoints previously open to any authenticated user now require recruiter role.

### Admin

- **Platform pay-equity dashboard** — see above.
- **Hard-delete endpoint for users** (`12d9c1a`) — Admin can now hard-delete (with `deleteUser` in storage).
- **Fraud watchlist** (`0813262`) — Entry updates include reason; usage tracking + quota management added.

### Security

- **3 Dependabot patches** (`2491acf`) — April 18.
- **Token generation** (`7c376db`) — `crypto.randomBytes` replaces predictable token sources for share/access tokens.
- **Cookie hygiene** (`0f81eb6`) — Untracked localhost `cookies*.txt` session files from repo.
- **Role-gate AI endpoints** (`f31a58a`) — See Recruiter section.

### Infrastructure & CI

- **SQL migration runner** (`1ac3966`) — Proper runner auto-applies `migrations/*.sql` on boot.
- **Feature-table migrations awaited** (`af0fabb`) — `/readyz` now waits for feature-table migrations before flipping healthy.
- **Pre-merge CI** (`0d90216`) — PR checks: tsc + static-analysis + smoke e2e.
- **Readiness endpoint** (`621259d`) — Added for E2E test reliability.
- **Runbooks** (`3778dcf`) — New runbooks for secrets rotation, git history scrub, migration rename.
- **Audit reports** (`bc038c2`) — Comprehensive security, SEO, and stubs audit reports landed.
- **19-day launch readiness audit** (`74168bb`) — Document added.

### Billing

- **Centralized pricing config** (`48542e6`, `cc94cfc`) — Pricing in shared module; feature entitlements seeding links features to plans.
- **Checkout ID persistence** (`570b679`) — Retained across payment flow retries.
- **Pay equity mutation improvements** — B-BBEE total score auto-calc (`b9885d0`).
- **Unified AI Governance & billing registries** (`9cd04fd`) — Single registry across Governance and billing.

### Models

- **Claude Opus 4.7** (`d200525`, 2026-04-18) — Model ID updated across all components. Previously 4.6.

## Pages to Update

- [[01-entities/sebenza-hub]] — table count 794→820, migration count 28→37, Claude model 4.7
- [[01-entities/whatsapp-bot]] — Linda now genuinely AI-powered (Phase 0–6); Flow Builder runtime authoritative; magic link auth; 7 cron triggers
- [[01-entities/individual-user]] — 9 previously-demo areas now real-data
- [[01-entities/recruiter-user]] — team management, smart match, AI candidate search, benchmark endpoints real
- [[01-entities/admin-user]] — platform pay-equity dashboard, hard-delete users, fraud watchlist enhancements
- [[02-concepts/whatsapp-integration]] — AI expansion, Flow Builder maturity, magic link
- [[02-concepts/ai-features]] — WhatsApp AI as first-class feature, smart match, RAG on wiki_pages
- [[02-concepts/compliance]] — POPIA AI consent now enforced (was a gap in the April 11 audit), privacy page live, demographic opt-in
- [[02-concepts/billing-system]] — centralized pricing, unified entitlements
- [[02-concepts/tech-stack]] — Claude Opus 4.7, SQL migration runner on boot

## Contradictions Resolved

The [[09-sources/sa-ai-policy-compliance-review-2026-04-11]] flagged AI-specific POPIA consent as a critical gap. **Partially resolved** in this sync:
- AI processing consent now **enforced** at `POST /api/applications` (`34ef400`).
- AI disclosure now **visible** on public job detail pages (`2b4f7bb`) and in QuickApplyDialog (`ca7442e`).
- Privacy Policy page live at `/privacy` (`02fc293`).
- Signup consent now **persists** (`bb48a71`).

Remaining gaps (still outstanding): POPIA Section 71 automated-decision notification + representation mechanism, bias audit operationalisation.

## Open Questions Raised

- Are any of the 7 cron-based flow triggers running in production, or seeded-only?
- What's the fallback rate now that intent AI is default-on (Phase 1)? Is it better than the pre-Phase-1 rate?
- Does RAG-on-`wiki_pages` reference the product wiki (this vault) or a codebase-internal `wiki_pages` table? If the latter, what seeds it?
- Which AI provider powers each Phase (intent classification, summarisation, translation, reply drafts)?
- Has the pay-equity dashboard been populated with real data, or is it schema-ready only?

## References

- [[09-sources/repo-audit-2026-04-07]] — Prior full audit (baseline)
- [[log.md]] — See 2026-04-11 sync entry for the previous delta
